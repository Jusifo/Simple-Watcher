# 1. Erstellen Sie ein Verzeichnis im /work-Laufwerk (falls es noch nicht existiert)
mkdir -p /work/jlusifo/pw-browsers

# 2. Setzen Sie die Umgebungsvariable und installieren Sie playwright
PLAYWRIGHT_BROWSERS_PATH=/work/jlusifo/pw-browsers pip install playwright

# 3. (Optional) Installieren Sie die restlichen requirements, falls noch nicht geschehen
# (Playwright wurde bereits im vorherigen Schritt installiert)
pip install -r requirements.txt